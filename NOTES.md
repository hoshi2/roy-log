# 開発の経緯メモ（roy-log / 旧pattern-radar）

2026年6〜7月に構築。当時はReact + Vite + GitHub Pagesで作った3本のうちの1本だった
（他の2本は `shukan-log` と `money-log`）。現在は単一HTMLファイル構成。

- 旧リポジトリ名は `pattern-radar`。2026-09-20 に `roy-log` へ改名
- 2026-07-08 から 2026-09-20 まで、このリポジトリの中に Trade OS の単一HTML版が
  `trade-os/index.html` として同居し、`hoshi2.github.io/roy-log/trade-os/` で公開されていた。
  2026-09-20 に独立リポジトリ `hoshi2/trade-os`（公開URL `hoshi2.github.io/trade-os/`）へ
  移し、こちらからは削除した。ロイログ本体からのリンクは元々無い

アプリの中身の説明は `README.md` と `CLAUDE.md` にある。

出典：`rules-docs/docs/webapp.md` を2026-09-20にアプリごとに分割したもの。
